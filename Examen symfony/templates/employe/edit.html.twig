{% extends 'base.html.twig' %}
{% block title %}Modifier {{ employe.nom }}{% endblock %}

{% block body %}
<h2>Modifier {{ employe.prenom }} {{ employe.nom }}</h2>
<div class="card card-body mt-3">
    {{ form_start(form) }}
        {{ form_row(form.matricule) }}
        {{ form_row(form.nom) }}
        {{ form_row(form.prenom) }}
        {{ form_row(form.email) }}
        {{ form_row(form.dateNaissance) }}
        {{ form_row(form.genre) }}
        {{ form_row(form.departement) }}
        {{ form_row(form.poste) }}
        <button type="submit" class="btn btn-warning">Mettre a jour</button>
        <a href="{{ path('employe_index') }}" class="btn btn-secondary">Annuler</a>
    {{ form_end(form) }}
</div>
{% endblock %}
